<div class="col-xl-3 col-md-6">
    <div class="dashboard-card">
        <div>
            <p class="dashboard-card-title">{{ $title }}</p>
            <h3 class="dashboard-card-value">{{ $value }}</h3>
        </div>

        <div class="dashboard-card-icon {{ $bg }}">
            <i class="bi {{ $icon }}"></i>
        </div>
    </div>
</div>
